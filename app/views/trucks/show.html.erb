<h1> Individual truck 🚚 </h1>

<h2><%= @truck.title %></h2>
<p><%= @truck.price %></p>
<p><%= @truck.description %></p>
<p><%= @truck.postcode %></p>

<h2><%= link_to "Make a new booking",
  new_truck_booking_path(@truck)
%></li> </h2>

<p><strong>Bookings</strong></p>
<ul class="list-group">
  <% @truck.bookings.each do |booking| %>
    <li class="list-group-item"><p> start date</p><%= booking.start_date%></li>
    <li class="list-group-item"><p> end date</p><%= booking.end_date%></li>
    <li><%= link_to "Update",
  edit_booking_path(@truck)
%></li>
    <li><%= link_to "Delete Booking",
  booking_path(@truck),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%></li>
  <% end %>
</ul>
<br>

<li><%= link_to "Update",
  edit_truck_path(@truck)
%></li>


<li><%= link_to "Delete",
  truck_path(@truck),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%></li>

<li><%= link_to "Return to list of all Trucks",
  trucks_path
 %></li>
